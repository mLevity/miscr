# Производительность, безопасность и эксплуатация
## Измеримые бюджеты
| Показатель | Цель |
|---|---|
| LCP | ≤2.5 s на75-м перцентиле при доступном полевом измерении |
| INP | ≤200 ms на75-м перцентиле |
| CLS | ≤0.1 |
| Поиск/фильтр на420 семействах | ≤100 ms после debounce в принятом тестовом профиле |
| Первый JS каталога | ориентир≤200 KiB gzip, инструменты отдельными chunks |
| Первый экран ассетов | ориентир≤1.5 MiB с оптимизированными карточками |
| Пересчёт одиночного damage | ≤16 ms чистое ядро |
| Серия100000 rebonus | worker; UI остаётся отзывчивым, отмена≤250 ms |
| Длинная операция в main thread | избегать задач>50 ms |

Core Web Vitals thresholds взяты из web.dev; остальные бюджеты — требования этого проекта, а не стандарты. Профиль лабораторного теста фиксируется: устройство/браузер, throttling сети и CPU, build commit, cold/warm cache. Не объявлять Lighthouse score доказательством удобства реальных пользователей. [Web Vitals](https://web.dev/articles/vitals)

## Загрузка и память
Первым приходит компактный индекс без lore/всех spell descriptions. Карточки грузят thumbnails нужной плотности через srcset; width/height или aspect-ratio предотвращает сдвиги. Первые видимые арты без lazy, остальные lazy + async decode. CSS background с сотнями больших PNG запрещён. Оригинальные ассеты сохраняются как source, production-версии создаются отдельно с manifest.

При большом каталоге — виртуализация сетки или страничное «Показать ещё» с сохранением положения. Не обязательно вводить тяжёлый virtualizer для420 записей без измерения, но нельзя держать1680 full-size sprites в DOM. Изображения карт загружаются по одной локации. Кэш decoded maps ограничен. Для будущего большого каталога индекс строится worker; компонент не фильтрует многократно весь JSON на каждый render.

В rebonus worker получает rules и seed, возвращает прогресс раз в100–250 ms и агрегаты. Не отправлять postMessage на каждую попытку. Для отмены — batch processing с проверкой флага или завершение worker; использование SharedArrayBuffer не обязательно и не должно заставлять включать cross-origin isolation ради простой задачи. `Math.random` допустим для локальной демонстрации, seeded RNG нужен для replay; RNG симулятора не предназначен для денежных ставок или выдачи игровых наград.

## Недоверенный ввод
Опасны: импортируемый JSON; заметки; search/query string; внешние данные справочника; имена ассетов; будущие API mutations. Всё это текст/структура, не исполняемый код. Рендерить названия через textContent/React escaping. Не использовать innerHTML для описаний без строгого sanitization; в MVP rich HTML из данных запрещён.

JSON.parse не вызывает код, но последующие merge могут вызвать prototype pollution. Разрешённые ключи выбираются явно; ключи `__proto__`, `prototype`, `constructor` в импортируемых объектах отклоняются на всех уровнях. Не использовать бесконтрольные рекурсивные Object.assign/merge. Ограничить размер/глубину/число элементов; сверять enum, IDs, числа finite и диапазоны. Не преобразовывать пустую строку в0 как валидный stat.

URL ресурсов: относительные пути из trusted asset manifest. Запрещены `javascript:`, `file:`, произвольные `data:` и внешние URL из пользовательского импорта. Не давать полю «фон» грузить любой URL с cookies. Будущий серверный importer не принимает произвольный URL от обычного пользователя; иначе возникает SSRF. Пути ассетов не должны содержать `..`, абсолютный путь или path separator от imported key.

## Политика браузера
Проектный пример CSP для production без inline JS:
```text
default-src 'self'; script-src 'self'; style-src 'self';
img-src 'self' blob:; font-src 'self'; connect-src 'self';
worker-src 'self'; object-src 'none'; base-uri 'self';
frame-ancestors 'none'; form-action 'self'
```

Это исходная политика, которую нужно протестировать на фактическом build. Для динамического позиционирования карты допустима отдельная `style-src-attr 'unsafe-inline'` либо безопасная схема CSSOM; не расширять script-src до unsafe-inline ради удобства. Если шрифты/ассеты размещаются на своём CDN, добавить только точные origins. Внутренние автономные HTML-демо ZIP используют inline scripts для file:// и не являются production CSP-шаблоном. [MDN CSP](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/CSP)

Production headers: X-Content-Type-Options:nosniff; Referrer-Policy:strict-origin-when-cross-origin; Permissions-Policy для отключения ненужных camera/microphone/geolocation. HTTPS; HSTS только после корректной настройки HTTPS на целевом домене. Будущие auth cookies: HttpOnly, Secure, SameSite с CSRF-защитой по выбранной схеме. Bearer credentials не хранятся в коллекционном JSON и не выводятся в логи.

## Будущий сервер
Проверять права на каждом mutation и ownership каждой личной записи. Rate limit на import, search-intensive и admin; размер тела запросов; SQL с параметрами; транзакции и optimistic concurrency. Админка отдельна логически и защищена сервером. CORS не является авторизацией. Роль из client state или imported file игнорируется. Editor редактирует только черновик; публикация новой datasetVersion атомарна и журналируется.

## Данные и приватность
В MVP не нужны игровые credentials, email, пароль, friend request и доступ к аккаунту. Не добавлять аналитику, которая отправляет всю коллекцию или произвольные заметки. Технические ошибки фиксируют код операции и версию, без содержимого личных профилей. Если позже добавляется аналитика, определяются события и основания отдельно.

Локальная коллекция не шифруется обещанием «данные защищены»: любой код того же origin при XSS может её прочитать. Поэтому текстовый рендер, строгие зависимости и CSP важнее фиктивного client-side шифрования с ключом рядом. Экспорт — пользовательский файл; UI объясняет, что его нужно сохранить самостоятельно. IndexedDB обеспечивает транзакционные асинхронные операции, но не гарантирует вечное хранение браузером. [MDN IndexedDB](https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API)

## Надёжность и доступность
Error boundary на уровне feature, retry только чтения с ограничением и backoff. Не делать бесконечный retry отсутствующего JSON. Offline/slow/error состояния отличаются. Клавиатура, screen reader, контраст и управление фокусом проверяются по применимым WCAG2.2 критериям; используемые44px hit areas — проектная цель удобства, не утверждение, что каждый такой размер обязателен во всех критериях AA. [WCAG Quick Reference](https://www.w3.org/WAI/WCAG22/quickref/)

Изображения сторонних карт и предметов имеют provenance; публичная доступность не считается лицензией. Перед публичным выпуском владелец проекта определяет право использования и кредитование; эта задача не требует от offline-исполнителя разыскивать неизвестного правообладателя. Не удалять credits и не заявлять сайт официальным без основания.
